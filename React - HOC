import React from 'react';

const HOC = (WrapperComponent) => {
  return function (props) {
    const a = true;
    if (a === true) {
      return <h1>Yes I am from HOC</h1>
    }
    return <WrapperComponent {...props} />
  }
}

export default HOC;

const updatedFunctionality = HOC(MyComponent);

A function takes an existing component and returns a new component with enhanced functionality.
